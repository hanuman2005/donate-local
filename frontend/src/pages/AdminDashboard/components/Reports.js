import React from "react";

const Reports = () => (
  <div>
    <h2>Reports Management</h2>
    <p>
      View, filter, and resolve reports on users, listings, and reviews here.
    </p>
    {/* TODO: Table of reports with actions (resolve, suspend, remove) */}
  </div>
);

export default Reports;
