import React from "react";

const Verifications = () => (
  <div>
    <h2>User Verification</h2>
    <p>Review and process pending user verification requests.</p>
    {/* TODO: List of requests with approve/reject actions */}
  </div>
);

export default Verifications;
