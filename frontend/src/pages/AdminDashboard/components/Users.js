import React from "react";

const Users = () => (
  <div>
    <h2>User Management</h2>
    <p>
      Search, view, suspend, or warn users. See user stats and trust scores.
    </p>
    {/* TODO: User table with actions and stats */}
  </div>
);

export default Users;
